# 第1周练习题

## Python基础练习

### 1. Python基本语法练习

请完成以下Python代码练习：

**题目1：** 编写一个函数，接受一个整数列表作为参数，返回其中所有偶数的平方和。

```python
def sum_of_even_squares(numbers):
    # 在这里编写你的代码
    pass

# 测试用例
print(sum_of_even_squares([1, 2, 3, 4, 5, 6]))  # 应该输出 56 (2^2 + 4^2 + 6^2 = 4 + 16 + 36)
```

**题目2：** 编写一个函数，接受一个字符串列表，返回其中最长的字符串。

```python
def find_longest_string(strings):
    # 在这里编写你的代码
    pass

# 测试用例
print(find_longest_string(["cat", "dog", "elephant", "bat"]))  # 应该输出 "elephant"
```

### 2. 面向对象编程练习

定义一个名为 `Rectangle` 的类，具有以下属性和方法：

- 属性：长度(length)和宽度(width)
- 方法：计算面积(area)、计算周长(perimeter)、判断是否为正方形(is_square)

```python
class Rectangle:
    def __init__(self, length, width):
        # 初始化代码
        pass
  
    def area(self):
        # 计算并返回面积
        pass
  
    def perimeter(self):
        # 计算并返回周长
        pass
  
    def is_square(self):
        # 判断是否为正方形，是则返回True，否则返回False
        pass

# 测试用例
rect = Rectangle(5, 3)
print(rect.area())        # 应该输出 15
print(rect.perimeter())   # 应该输出 16
print(rect.is_square())   # 应该输出 False

square = Rectangle(4, 4)
print(square.is_square()) # 应该输出 True
```

### 3. 函数和模块练习

创建一个模块 `math_utils.py`，包含以下函数：

- `is_prime(n)` - 判断一个数是否为质数
- `factorial(n)` - 计算一个数的阶乘
- `gcd(a, b)` - 计算两个数的最大公约数

然后编写一个程序来使用这些函数。

## 数学基础练习

### 1. 向量和矩阵运算

给定两个向量 `u = [2, 3, 1]` 和 `v = [1, -1, 4]`，计算：

1. 向量加法: u + v
2. 向量点积: u · v
3. 向量u的模长

给定两个矩阵:

```
A = [[1, 2],
     [3, 4]]

B = [[5, 6],
     [7, 8]]
```

计算矩阵乘法: A × B

### 2. 导数和偏导数

计算下列函数的导数：

1. f(x) = 3x^2 + 2x - 5
2. f(x) = sin(x) + cos(x)
3. f(x, y) = x^2*y + 3xy^2，计算∂f/∂x和∂f/∂y

### 3. 概率分布

解答以下问题：

1. 一枚公平硬币投掷3次，恰好出现2次正面的概率是多少？
2. 在标准正态分布中，求P(-1 ≤ Z ≤ 1)的近似值。
3. 描述泊松分布和二项分布之间的关系。

## 实践任务

### 任务1：Python基础练习题

完成以下10道练习题：

1. 编写一个程序，输出斐波那契数列的前20项
2. 编写一个函数，判断一个字符串是否为回文
3. 编写一个程序，实现冒泡排序算法
4. 编写一个函数，找出列表中的第二大元素
5. 编写一个程序，实现简单的计算器功能
6. 编写一个函数，将十进制数转换为二进制数
7. 编写一个程序，统计文本文件中每个单词的出现次数
8. 编写一个函数，实现矩阵的转置
9. 编写一个程序，生成并打印九九乘法表
10. 编写一个函数，实现简单的密码加密和解密

### 任务2：矩阵运算程序

实现一个矩阵类 `Matrix`，支持以下操作：

- 矩阵加法
- 矩阵乘法
- 矩阵转置
- 计算行列式（2x2和3x3矩阵）

### 任务3：概率分布程序

编写一个程序，包含以下功能：

- 生成指定参数的二项分布随机数
- 计算正态分布在给定区间的概率密度
- 绘制常见概率分布的图形（可选）

## 学习资源检查

在完成上述练习后，请检查是否已完成以下学习资源：

- [ ] 《Python编程：从入门到实践》相关章节阅读笔记
- [ ] 3Blue1Brown的微积分和线性代数视频观看笔记
- [ ] Khan Academy线性代数课程的前3章练习题

## 自我评估问题

回答以下问题来评估你的掌握程度：

1. 你能否不参考资料独立编写一个Python类？请尝试编写一个表示复数的类。
2. 解释向量点积的几何意义是什么？它在实际中有哪些应用？
3. 什么是偏导数？它与普通导数有什么区别？
4. 你遇到的最大学习挑战是什么？你是如何克服的？
5. 哪些概念还需要进一步学习和练习？
